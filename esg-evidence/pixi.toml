[project]
name = "esg-evidence"
version = "1.0.0"
description = "Evidence.dev dashboard with sample data initialization"
channels = ["conda-forge"]
platforms = ["osx-arm64", "osx-64", "linux-64"]

[dependencies]
python = ">=3.11"
duckdb = ">=0.10.0"

[tasks]
init-data = "python init_sample_data.py"
